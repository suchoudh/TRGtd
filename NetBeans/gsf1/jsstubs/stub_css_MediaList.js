var MediaList = {
  // This is just a stub for a builtin native JavaScript object.
/**
 * Adds the medium newMedium to the
 * end of the list. If the newMedium is already used, it
 * is first removed.
 * @param {String} newMedium of type DOMString The new medium to add.
 * @type void
 */
appendMedium: function(newMedium) {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Deletes the medium indicated by oldMedium from the list.
 * @param {String} oldMedium of type DOMString The medium to delete in the media list.
 * @type void
 */
deleteMedium: function(oldMedium) {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Returns the index th in the list.
 * If index is greater than or equal to the number of
 * media in the list, this returns null .
 * @param {Number} index of type unsigned long Index into the collection.
 * @return DOMString The medium at the index th position in the MediaList , or null if that is not a valid
 * index.
 * @type String
 */
item: function(index) {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * The number of media in the list. The range of valid media is
 * 0 to length-1 inclusive.
 * @type Number
 */
length: undefined,
/**
 * The parsable textual representation of the media list. This is
 * a comma-separated list of media.
 * @type String
 */
mediaText: undefined,
};

