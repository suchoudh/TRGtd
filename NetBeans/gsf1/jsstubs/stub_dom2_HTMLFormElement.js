var HTMLFormElement = Object.extend(new HTMLElement(), {
  // This is just a stub for a builtin native JavaScript object.
/**
 * List of character sets supported by the server. See the 
 * accept-charset attribute definition in HTML 4.01.
 * @type String
 */
acceptCharset: undefined,
/**
 * Server-side form handler. See the 
 * action attribute definition in HTML 4.01.
 * @type String
 */
action: undefined,
/**
 * Returns a collection of all form control elements in the
 * form.
 * @type HTMLCollection
 */
elements: undefined,
/**
 * The content type of the submitted form, generally
 * "application/x-www-form-urlencoded". See the 
 * enctype attribute definition in HTML 4.01. 
 * @type String
 */
enctype: undefined,
/**
 * The number of options in this SELECT.
 * @type Number
 */
length: undefined,
/**
 * HTTP method [IETF RFC 2616] used to submit
 * form. See the 
 * method attribute definition in HTML 4.01.
 * @type String
 */
method: undefined,
/**
 * The frame name (object of the target attribute).
 * See the 
 * name attribute definition in HTML 4.01.
 * @type String
 */
name: undefined,
/**
 * Restores a form element's default values. It
 * performs the same action as a reset button.
 * @type void
 */
reset: function() {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Submits the form. It performs the same action
 * as a submit button.
 * @type void
 */
submit: function() {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Frame to render the resource in. See the 
 * target attribute definition in HTML 4.01.
 * @type String
 */
target: undefined,
});

