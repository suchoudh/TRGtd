var FlashPlayer = {
  // This is just a stub for a builtin native JavaScript object.
/**
 * Get a movie variable
 * @type String
 * @param {String} variableName The name of the variable
*/
GetVariable: function(variableName) {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Jump to a specified frame
 * @type void
 * @param {Number} frameNumber
*/
GotoFrame: function(frameNumber) {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Return true if the movie is playing
 * @type Boolean
*/
IsPlaying: function() {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Load a given movie into a given layer
 * @type void
 * @param {Number} layer Layer to display in
 * @param {String} url The URL of the movie
*/
LoadMovie: function(layer,url) {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Move the movie viewport
 * @type void
 * @param {Number} dx Horizontal pan
 * @param {Number} dy Vertical pan
 * @param {Number} mode 0 for pixels, 1 for percentages
*/
Pan: function(dx,dy,mode) {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Return the percentage of the movie that has been loaded
 * @type Number
*/
PercentLoaded: function() {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Play the movie
 * @type void
*/
Play: function() {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Rewind the movie
 * @type void
*/
Rewind: function() {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Set a movie variable
 * @type void
 * @param {String} name The name of the variable
 * @param {String} value The value of the vairable
*/
SetVariable: function(name,value) {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Set the viewport
 * @type void
 * @param {Number} left Left edge of viewport in twips
 * @param {Number} top Top edge of viewport in twips
 * @param {Number} right Right edge of viewport in twips
 * @param {Number} bottom Bottom edge of viewport in twips
*/
SetZoomRect: function(left,top,right,bottom) {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Stop playing the movie
 * @type void
*/
StopPlay: function() {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Return the framecount for the movie
 * @type Number
*/
TotalFrames: function() {
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Zoom in or out
 * @param {Number} percentage Scale amount, or 0 to resize to full
*/
Zoom: function(percentage) {
  // This is just a stub for a builtin native JavaScript object.
},
};

