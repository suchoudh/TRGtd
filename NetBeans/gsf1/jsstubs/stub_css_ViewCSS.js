var ViewCSS = Object.extend(new AbstractView(), {
  // This is just a stub for a builtin native JavaScript object.
/**
 * This method is used to get the computed style
 * as it is defined in [ CSS2 ].
 * @param {Element} elt of type Element The element whose style is to be computed. This parameter
 * cannot be null.
 * @param {String} pseudoElt of type DOMString The pseudo-element or null if none.
 * @return CSSStyleDeclaration The computed style. The CSSStyleDeclaration is read-only and contains only absolute values.
 * @type CSSStyleDeclaration
 */
getComputedStyle: function(elt, pseudoElt) {
  // This is just a stub for a builtin native JavaScript object.
},
});

