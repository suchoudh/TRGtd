var DOMImplementationCSS = Object.extend(new DOMImplementation(), {
  // This is just a stub for a builtin native JavaScript object.
/**
 * Creates a new CSSStyleSheet .
 * @param {String} title of type DOMString The advisory title. See also the Style Sheet
 * Interfaces section.
 * @param {String} media of type DOMString The comma-separated list of media associated with the new style
 * sheet. See also the Style Sheet
 * Interfaces section.
 * @return CSSStyleSheet A new CSS style sheet.
 * @type CSSStyleSheet
 */
createCSSStyleSheet: function(title, media) {
  // This is just a stub for a builtin native JavaScript object.
},
});

