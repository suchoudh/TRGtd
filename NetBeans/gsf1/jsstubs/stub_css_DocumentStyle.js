var DocumentStyle = {
  // This is just a stub for a builtin native JavaScript object.
/**
 * A list containing all the style sheets explicitly linked into
 * or embedded in a document. For HTML documents, this includes
 * external style sheets, included via the HTML 
 * LINK element, and inline 
 * STYLE elements. In XML, this includes external style
 * sheets, included via style sheet processing instructions (see [XML-StyleSheet]).
 * @type StyleSheetList
 */
styleSheets: undefined,
};

