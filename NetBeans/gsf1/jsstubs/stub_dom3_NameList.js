var NameList = {
  // This is just a stub for a builtin native JavaScript object.
/**
 * Test if a name is part of this NameList .
 * @param {String} str of type DOMString The name to look for.
 * @return boolean true if the name has been found, false otherwise.
 */
contains: function(str) { // COMPAT=IE7|FF1|FF2|FF3|OPERA|SAFARI2|SAFARI3|KONQ
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Test if the pair namespaceURI/name is part of
 * this NameList .
 * @param {String} namespaceURI of type DOMString The namespace URI to look for.
 * @param {String} name of type DOMString The name to look for.
 * @return boolean true if the pair namespaceURI/name has been found, false otherwise.
 */
containsNS: function(namespaceURI, name) { // COMPAT=IE7|FF1|FF2|FF3|OPERA|SAFARI2|SAFARI3|KONQ
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Returns the index th name item in
 * the collection.
 * @param {Number} index of type unsigned long Index into the collection.
 * @return DOMString The name at the index th position in the NameList , or null if there is no name for
 * the specified index or if the index is out of range.
 */
getName: function(index) { // COMPAT=IE7|FF1|FF2|FF3|OPERA|SAFARI2|SAFARI3|KONQ
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * Returns the index th namespaceURI
 * item in the collection.
 * @param {Number} index of type unsigned long Index into the collection.
 * @return DOMString The namespace URI at the index th position in the NameList , or null if there is no name for
 * the specified index or if the index is out of range.
 */
getNamespaceURI: function(index) { // COMPAT=IE7|FF1|FF2|FF3|OPERA|SAFARI2|SAFARI3|KONQ
  // This is just a stub for a builtin native JavaScript object.
},
/**
 * The number of 16-bit
 * units that are available through data and the
 * substringData method below. This may have the value
 * zero, i.e., CharacterData nodes may be
 * empty.
 * @type Number
 */
length: undefined, // COMPAT=IE7|FF1|FF2|FF3|OPERA|SAFARI2|SAFARI3|KONQ
};

